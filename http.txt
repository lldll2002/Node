<<https>> 웹 서버에 SSL 암호화를 추가하는 모듈
https 오고가는 데이터를 암호화, 중간에 다른 사람이 요청을 가로채더라도 내용을 확인할 수 없음
http만 사용하면 보안적인 문제가 있다.
실제로 sequrity가 추가(인증서, 비번 등등) 되어야 하는데 주로 배포단계에서 많이 설정
!! http 보다 좀 더 보안이 특화가 됐다 !!



http2>>
SSL 암호화와 더불어 최신 HTTP 프로토콜인 http/2를 사용하는 모듈
http/1.1은 S와 P 둘 다 켤 수 있다
http/2는 S(보안)만 사용 가능. 요즘 필수라서.
http/3 아직 개발중 보안측면 강화

http/2는 바이너리파일(버퍼와 같은 것)로 보내기 때문에 웹 속도가 개선 됨